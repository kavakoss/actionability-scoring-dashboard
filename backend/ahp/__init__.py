"""AHP weight generation for the actionability scoring instrument.

Run ``python -m ahp.run_ahp`` to regenerate ``backend/weights.json`` and
``backend/data/mitre_traceability.csv`` from the pairwise matrices.
"""
